#!/bin/bash
# This script will check for and create if not present symlinks for common jars across the different server directories,
# such as common plugins, and the server jar itself.

# TODO: This script

exit 1